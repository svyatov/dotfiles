-d postgresql          # PostgreSQL WTF!
--skip-test            # skips making the test unit folders (because RSpec!)
--skip-active-storage  # skips Active Storage (because Shrine!)
--skip-action-mailbox  # skips Action Mailbox gem (because we don't need inbox for emails)
